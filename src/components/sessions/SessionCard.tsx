import { Session } from "@/generated/prisma/client";

export const AdminSessionCard = ({ session }: { session: Session }) => {
    return (
        <div>
            TODO: SESSION CARD
        </div>
    )
}